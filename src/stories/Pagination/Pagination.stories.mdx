import { Story, Canvas, ArgsTable, Meta } from "@storybook/addon-docs";

import { Pagination } from '../../components';

<Meta
    title="Components/Pagination"
    argTypes={{
        className: {
            description: 'Override or extend the styles applied to the component.',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
            }
        },
        size: {
            description: 'The size of the component',
        },
        dataTestId: {
            description: 'The id of the component for test',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
            }
        },
        items: {
          description: 'The array of page numbers',
          table: {
              defaultValue: {
                  summary: 'undefined',
              },
          }
        }
    }}
    component={Pagination}
/>

export const Template = (args) => <Pagination {...args} />;

## Pagination

### Pagination
<Canvas>
    <Story
        name="Demo"
        args={{
            size: 'lg',
            className: 'pagination',
            dataTestId: 'pagination',
            items: [1, 2, 3, 4, 5, 6],
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Demo" />

### Pagination sizes

<Canvas>
    <Story id='components-pagination--small' />
    <Story id='components-pagination--large' />
</Canvas>
