import { Story, Canvas, ArgsTable, Meta } from '@storybook/addon-docs';

import { TableName, TextField, TableControl } from '../../../../components';

<Meta
  title="Components/Table/TableControl"
  component={TableControl}
/>

export const TableControlTemplate = (args) => <TableControl {...args} />;

# TableControl

TableControl contains table name and text search field.

### Default
  <Canvas>
    <Story id='components-table-tablecontrol--default' />
  </Canvas>

## Demo
<Canvas>
  <Story
    name="Demo"
    args={{
      children: (
        <>
          <TableName badgeNumber="200"> Demo table control</TableName>
          <TextField controlId="search" placeholder="search" onChange={() => {}} showDefaultIcon /> </>
      ),
      className: 'custom-tablecontrol',
      dataTestId: 'test-tablecontrol-id'
    }}
  >
    {TableControlTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Demo" />
