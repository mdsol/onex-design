import { Story, Canvas, ArgsTable, Meta } from '@storybook/addon-docs';

import { Tooltip, Badge } from '../../../../components';
import * as stories from './Tooltip.stories.jsx';

<Meta
  title="Onex/Components/Tooltip"
  argTypes={{
    className: {
      description: 'Override or extend the styles applied to the component.',
      table: {
        defaultValue: {
          summary: 'undefined',
        },
      },
    },
    id: {
      description: 'An html id attribute, necessary for accessibility ',
      table: {
        defaultValue: {
          summary: 'undefined',
        },
      },
    },
    children: {
      description:
        'The react component or DOM element relative to which the tooltip will be shown. ',
      table: {
        defaultValue: {
          summary: 'undefined',
        },
      },
    },
    delay: {
      description: 'A millisecond delay amount to show and hide the Overlay once triggered. ',
      table: {
        defaultValue: {
          summary: 'undefined',
        },
      },
    },
    defaultShow: {
      description: 'The initial visibility state of the Overlay.',
      table: {
        defaultValue: {
          summary: 'false',
        },
      },
    },
    show: {
      description:
        'controlled by: <b>onToggle</b>, initial prop: <b>defaultShow</b> The visibility of the Overlay. <b>show</b> is a controlled prop so should be paired with <b>onToggle</b> to avoid breaking user interactions. Manually toggling show does not wait for delay to change the visibility.',
      table: {
        defaultValue: {
          summary: 'false',
        },
      },
    },
    trigger: {
      description:
        "Specify which action or actions trigger Overlay visibility. 'hover' | 'click' |'focus' | Array<'hover' | 'click' |'focus'>",
      table: {
        defaultValue: {
          summary: '[hover, focus]',
        },
      },
    },
    placement: {
      description: 'Specify which action or actions trigger Overlay visibility',
    },
    tooltipChildren: {
      description: 'The content of the tooltip',
      table: {
        defaultValue: {
          summary: 'undefined',
        },
      },
    },
    onToggle: {
      description:
        'A callback that fires when the user triggers a change in tooltip visibility. <b>onToggle</b> is called with the desired next <b>show</b>, and generally should be passed back to the <b>show</b> prop. <b>onToggle</b> fires after the configured delay',
      table: {
        defaultValue: {
          summary: 'undefined',
        },
      },
    },
    dataTestId: {
      description: 'The id of the component for test',
      table: {
        defaultValue: {
          summary: 'undefined',
        },
      },
    },
  }}
  component={Tooltip}
/>

export const Template = (args) => <Tooltip {...args} />;

## Tooltip

Tooltips display informative text when users hover over, focus on, or tap an element.

## Demo

<Canvas>
  <Story
    name="Demo"
    args={{
      id: 'demo-tooltip',
      variant: 'default',
      tooltipChildren: 'demo tooltip',
      className: 'onex-demo-tooltip',
      delay: { show: 100, hide: 1000 },
      defaultShow: false,
      dataTestId: 'demo-tooltip-id',
      trigger: 'hover',
      children: (
        <Badge type="default" variant="status">
          Hover to see
        </Badge>
      ),
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Demo" />

### Tooltip variants

<Canvas>
  <Story name="Default" story={stories.Default} />
</Canvas>

### Tooltip placement

<Canvas>
  <Story name="Left" story={stories.Left} />
  <Story name="Top" story={stories.Top} />
  <Story name="Right" story={stories.Right} />
  <Story name="Bottom" story={stories.Bottom} />
</Canvas>

### Tooltip trigger

<Canvas>
  <Story name="Hover" story={stories.Hover} />
  <Story name="Focus" story={stories.Focus} />
  <Story name="Click" story={stories.Click} />
</Canvas>

### Tooltip delay

<Canvas>
  <Story name="ShowDelay1000ms" story={stories.ShowDelay1000ms} />
  <Story name="HideDelay2000ms" story={stories.HideDelay2000ms} />
</Canvas>
