<!-- Typography.mdx -->
import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { Typography } from '../../../../components';

<Meta
  title="Typography"
  component={Typography}
/>

export const TypographyTemplate = (args) => <Typography {...args} />;

# Typography
Use typography to present your design and content as clearly and efficiently as possible.

## Style

### Alignment
<Canvas isColumn>
  <Story id='typography--center-align' />
  <Story id='typography--inherit-align' />
  <Story id='typography--justify-align' />
  <Story id='typography--left-align' />
  <Story id='typography--right-align' />
</Canvas>

### Variants

#### Headings
Supports from ```h1``` till ```h5```
<Canvas isColumn>
  <Story id='typography--h-1' />
  <Story id='typography--h-2' />
  <Story id='typography--h-3' />
  <Story id='typography--h-4' />
  <Story id='typography--h-5' />
</Canvas>

#### Body
The component renders a ```<p>``` element.
With variant ```body``` also used argument: ```href```.
Providing a ```href``` will render an ```<a>``` element.
Providing a ```semiBold```  argument will render an ```<p>``` element with width ```600```.

<Canvas isColumn>
  <Story id='typography--body' />
  <Story id='typography--body-semibold' />
  <Story id='typography--body-link' />
</Canvas>

#### Label
The component renders a ```<span>``` element.
With variant ```label``` also used argument: ```href```.
Providing a ```href``` will render an ```<a>``` element.
Providing a ```semiBold```  argument will render an ```<span>``` element with width ```600```.

<Canvas isColumn>
  <Story id='typography--label' />
  <Story id='typography--label-semibold' />
  <Story id='typography--label-link' />
</Canvas>

#### Caption
The component renders a ```<span>``` element.
With variant ```caption``` also used arguments: ```uppercase``` and ```href```.
Providing a ```href``` will render an ```<a>``` element.
Providing a ```semiBold```  argument will render an ```<span>``` element with width ```600```.
Providing a ```uppercase```  argument will render an ```<span>``` element with text-transform ```uppercase```.

<Canvas isColumn>
  <Story id='typography--caption' />
  <Story id='typography--caption-semibold' />
  <Story id='typography--caption-link' />
  <Story id='typography--caption-uppercase' />
</Canvas>


## Demo
<Canvas>
  <Story
    name="Demo"
    args={{
      align: 'center',
      children:'Body',
      className: 'custom-typography',
      dataTestId: 'test-typography-id',
      href: '#',
      marginBottom: false,
      noWrap: true,
      semiBold: true,
      paragraph: false,
      variant: 'body',
      uppercase: false,
    }}>
    >
    {TypographyTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Demo" />
