import { Story, Canvas, ArgsTable, Meta } from '@storybook/addon-docs';

import { DataGridName } from '../../../../../../components';

<Meta
  title="Onex/Components/DataGrid/DataGridName"
  argTypes={{
    badgeNumber: {
      description: 'The content of the Badge component',
      control: 'text',
    },
    children: {
      description: 'The content of the component',
      control: 'text',
    },
    className: {
      description: 'Name of the CSS class',
      control: 'text',
    },
    dataTestId: {
      description: 'Id of the component for testing',
      control: 'text',
    },
  }}
  component={DataGridName}
/>

export const DataGridNameTemplate = (args) => <DataGridName {...args} />;

# DataGridName

## Demo
<Canvas>
  <Story
    name="Demo"
    args={{
      badgeNumber: '250',
      children: 'Data grid name',
      className: 'custom-data-grid-name',
      dataTestId: 'test-data-grid-id'
    }}
  >
    {DataGridNameTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Demo" />
