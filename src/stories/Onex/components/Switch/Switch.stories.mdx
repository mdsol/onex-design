import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { Switch } from '../../../../components';
import * as stories from './Switch.stories.jsx';

<Meta
  title="Onex/Components/Switch"
  component={Switch}
/>

export const SwitchTemplate = (args) => <Switch {...args} />;


# Switch
Switches toggle the state of a single setting on or off.

## Basic switches
<Canvas>
  <Story name="Default" story={stories.Default} />
  <Story name="Checked" story={stories.Checked} />
  <Story name="Disabled" story={stories.Disabled} />
  <Story name="CheckedDisabled" story={stories.CheckedDisabled} />
</Canvas>

## With label
<Canvas>
  <Story name="WithLabel" story={stories.WithLabel} />
</Canvas>

## Controlled
You can control the switch with the ```checked``` and ```onChange``` props.
<Canvas>
  <Story name="Controlled">
    {() => {
      const [checked, setChecked] = React.useState(true);
      return (
        <>
          <Switch
                  checked={checked}
                  onChange={() => setChecked(!checked)}
                  id="custom-switch-checked"/>
        </>
      );
    }}
  </Story>
</Canvas>


## Demo

<Canvas>
  <Story
    name="Demo"
    args={{
      checked: true,
      className: 'custom-switch',
      dataTestId: 'test-switch-id',
      disabled: false,
      id: 'custom-demo-switch',
      label: 'Demo',
      onChange: () => {},
    }}>>
    >
    {SwitchTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Demo" />
