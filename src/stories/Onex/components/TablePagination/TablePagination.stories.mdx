import { Story, Canvas, ArgsTable, Meta } from "@storybook/addon-docs";

import { TablePagination } from '../../../../components';

<Meta
    title="Onex/Components/TablePagination"
    argTypes={{
        className: {
            description: 'Override or extend the styles applied to the component.',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
            }
        },
        dataTestId: {
            description: 'The id of the component for test',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
            }
        },
      rowsPerPageOptions: {
          description: 'The array of quantities of rows per page',
          table: {
              defaultValue: {
                  summary: [],
              },
          }
        },
        defaultRowsPerPage: {
          description: 'The default quantity of rows per page',
          table: {
              defaultValue: {
                  summary: 'undefined',
              },
          }
        },
        rows: {
          description: 'The quantity of rows',
          table: {
              defaultValue: {
                  summary: 'undefined',
              },
          }
        },
        previousPage: {
          description: 'Number of previous page',
          table: {
            defaultValue: {
              summary: 'undefined',
            },
          }
        },
        nextPage: {
          description: 'Number of next page',
          table: {
            defaultValue: {
              summary: 'undefined',
            },
          }
        },
        canPreviousPage: {
          description: 'If true, prev button is active',
          table: {
            defaultValue: {
              summary: false,
            },
          }
        },
      canNextPage: {
        description: 'If true, next button is active',
        table: {
          defaultValue: {
            summary: false,
          },
        }
      },
      lastRow: {
        description: 'Number of last row',
        table: {
          defaultValue: {
            summary: 'undefined',
          },
        }
      },
      setPageSize: {
        description: 'Callback fired when change quantity of rows per page',
      },
    }}
    component={TablePagination}
/>

export const Template = (args) => <TablePagination {...args} />;

## TablePagination

A TablePagination is a pagination for tables.

### TablePagination
<Canvas>
    <Story
        height="12rem"
        name="Demo"
        args={{
            className: 'table-pagination',
            dataTestId: 'table-pagination-id',
            rowsPerPageOptions: [5, 10, 20],
            defaultRowsPerPage: 10,
            rows: 100,
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Demo" />
