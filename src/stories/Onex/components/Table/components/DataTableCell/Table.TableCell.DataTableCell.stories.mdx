import { Story, Canvas, ArgsTable, Meta } from '@storybook/addon-docs';
import { Avatar, Table, TableBody, TableRow, TableCell, DataTableCell, Icon } from '../../../../../../components';
import * as stories from './Table.TableCell.DataTableCell.stories.jsx';

export const icons = { Star: <Icon>star</Icon>, Flag: <Icon>flag</Icon> };

<Meta
  title="Onex/Components/Table/TableCell/DataTableCell"
  argTypes={{
    children: {
      description: 'The content of the component',
    },
    className: {
      description: 'Name of the CSS class',
      control: 'text',
    },
    dataTestId: {
      description: 'Id of the component for testing',
      control: 'text',
    },
    icon: {
      description: 'Icon element',
      options: Object.keys(icons),
      mapping: icons,
      control: {
        type: 'select',
        labels: {
          ArrowUp: 'Up',
          ArrowDown: 'Down',
        },
      },
    },
    iconPosition: {
      description: 'Icon positioning relative to other elements',
      control: 'text',
    },
    img: {
      description: 'Img element, could be ```<Avatar>```',
    },
    subTitle: {
      description: 'Caption displayed at the bottom',
      control: 'text',
    },
  }}
  component={DataTableCell}
/>

export const DataTableCellTemplate = (args) => (
  <Table>
    <TableBody>
      <TableRow>
        <TableCell>
          <DataTableCell {...args}/>
        </TableCell>
      </TableRow>
    </TableBody>
  </Table>
);

# DataTableCell
DataTableCell component represents content of ```TableCell```.

## Demo
<Canvas>
  <Story
    name="Demo"
    args={{
      children: 'Clinical and Radiological Evaluation of Patients With',
      className: 'custom-datatablecell',
      dataTestId: 'test-datatablecell-id',
      icon: <Icon>star</Icon>,
      iconPosition: 'end',
      img:(
        <Avatar
          src="https://robohash.org/my-own-slug.png?size=300x300&set=set1"
          size="md"
          hoverDisabled
        />),
      subTitle: 'Vertebrobasilar Insufficiency'
    }}>
   >
    {DataTableCellTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Demo" />

<Canvas>
  <Story name="Default" story={stories.Default} />
</Canvas>
