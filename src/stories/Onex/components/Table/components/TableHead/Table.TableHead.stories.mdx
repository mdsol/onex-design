import { ExclamationDiamondFill } from 'react-bootstrap-icons';
import { Story, Canvas, ArgsTable, Meta } from '@storybook/addon-docs';
import { Table, TableHead, TableRow, TableCell, Check } from '../../../../../../components';
import * as stories from './Table.TableHead.stories.jsx';

<Meta
  title="Onex/Components/Table/TableHead"
  component={TableHead}
/>

export const TableHeadTemplate = (args) => (
  <Table>
    <TableHead {...args}/>
  </Table>
);

# TableHead

The component renders a ```<thead>``` element.

### Default
<Canvas>
  <Story name="Default" story={stories.Default} />
</Canvas>

## Demo
<Canvas>
  <Story
    name="Demo"
    args={{
      children: (
        <TableRow>
          <TableCell align="center" width="60px">
            <Check
              id="header-check"
              size="sm"
              onChange={() => {}}
            />
          </TableCell>
          <TableCell width="50%">Study name</TableCell>
          <TableCell>Study short name</TableCell>
          <TableCell>Phase</TableCell>
          <TableCell align="center" width="60px">
            <ExclamationDiamondFill />
          </TableCell>
        </TableRow>),
      className: 'custom-tablehead',
      dataTestId: 'test-tablehead-id'
    }}>
    {TableHeadTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Demo" />
