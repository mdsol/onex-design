import { Story, Canvas, ArgsTable, Meta } from "@storybook/addon-docs";

import { TablePagination } from '../../components';

<Meta
    title="Components/TablePagination"
    argTypes={{
        className: {
            description: 'Override or extend the styles applied to the component.',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
            }
        },
        size: {
            description: 'The size of the component',
        },
        dataTestId: {
            description: 'The id of the component for test',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
            }
        },
        rowsDividers: {
          description: 'The array of quantities of rows per page',
          table: {
              defaultValue: {
                  summary: 'undefined',
              },
          }
        },
        defaultRowsPerPage: {
          description: 'The default quantity of rows per page',
          table: {
              defaultValue: {
                  summary: 'undefined',
              },
          }
        },
        rows: {
          description: 'The quantity of rows',
          table: {
              defaultValue: {
                  summary: 'undefined',
              },
          }
        }
    }}
    component={TablePagination}
/>

export const Template = (args) => <TablePagination {...args} />;

## TablePagination

A TablePagination is a pagination for tables.

### TablePagination
<Canvas>
    <Story
        name="Demo"
        args={{
            size: 'lg',
            className: 'tablePagination',
            dataTestId: 'tablePagination',
            rowsDividers: [5, 10, 20, 25, 50],
            defaultRowsPerPage: 10,
            rows: 100,
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Demo" />

### TablePagination sizes

<Canvas>
    <Story id='components-tablepagination--small' />
    <Story id='components-tablepagination--large' />
</Canvas>
