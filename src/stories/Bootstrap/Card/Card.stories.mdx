import { Story, Canvas, ArgsTable, Meta, Stories } from '@storybook/addon-docs';

import { Card } from '../../../components';
import * as stories from './Card.stories.jsx';

<Meta
    title="Bootstrap/Components/Card"
    argTypes={{
        children: {
            description: 'The content of the component.',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
            }
        },
        bg: {
            description: 'Sets card background.',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
                type: {
                    summary: "'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'dark' | 'light'",
                }
            },
        },
        border: {
            description: 'Sets card border color.',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
                type: {
                    summary: "'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'dark' | 'light'",
                }
            },
        },
        text: {
            description: 'Sets card text color.',
            table: {
                defaultValue: {
                    summary: 'undefined',
                },
                type: {
                    summary: "'primary' | 'secondary' | 'success' | 'danger' | 'warning' | 'info' | 'dark' | 'light' | 'white' | 'muted'",
                }
            },
        },
        as: {
            description: 'You can use a custom element type for this component.',
            table: {
                defaultValue: {
                    summary: "<div\>",
                },
                type: {
                    summary: 'elementType'
                }
            }
        }
    }}
    component={Card}
/>

export const Template = (args) => <Card {...args} />;

## Card

Bootstrap’s cards provide a flexible and extensible content container with multiple variants and options.

Use `<Card.Body>` to pad content inside a `<Card>`.

Using `<Card.Title>`, `<Card.Subtitle>`, and `<Card.Text>` inside the `<Card.Body>` will line them up nicely.

`<Card.Link>`s are used to line up links next to each other.

`<Card.Text>` outputs `<p>` tags around the content, so you can use multiple `<Card.Text>`s to create separate paragraphs.

### Basic Example
<Canvas>
    <Story
        name="Demo"
        args={{
            bg: 'light',
            text: 'dark',
            border: 'dark',
            children: (
                <>
                    <Card.Header>Header</Card.Header>
                    <Card.Body>
                        <Card.Title>Card Title </Card.Title>
                        <Card.Text>
                            Some quick example text to build on the card title and make up the bulk of the
                            card`s content.
                        </Card.Text>
                    </Card.Body>
                </>
            ),
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Demo" />

## Background color type
You can change a card's appearance by changing their `<bg>`, and `<text>` props.

<Canvas>
    <Story name="Primary" story={stories.Primary} />
    <Story name="Secondary" story={stories.Secondary} />
    <Story name="Success" story={stories.Success} />
    <Story name="Warning" story={stories.Warning} />
    <Story name="Danger" story={stories.Danger} />
    <Story name="Info" story={stories.Info} />
    <Story name="Light" story={stories.Light} />
    <Story name="Dark" story={stories.Dark} />
</Canvas>

## Border and text color types

<Canvas>
    <Story name="PrimaryBorderAndText" story={stories.PrimaryBorderAndText} />
    <Story name="SecondaryBorderAndText" story={stories.SecondaryBorderAndText} />
    <Story name="SuccessBorderAndText" story={stories.SuccessBorderAndText} />
    <Story name="WarningBorderAndText" story={stories.WarningBorderAndText} />
    <Story name="DangerBorderAndText" story={stories.DangerBorderAndText} />
    <Story name="InfoBorderAndText" story={stories.InfoBorderAndText} />
    <Story name="LightBorderAndText" story={stories.LightBorderAndText} />
    <Story name="DarkBorderAndText" story={stories.DarkBorderAndText} />
</Canvas>

## Header and Footer

You may add a header by adding a `<Card.Header>` component.
A `<CardHeader>` can be styled by passing a heading element through the `<as>` prop

You may add a footer by adding a `<Card.Footer>` component.

<Canvas>
    <Story name="HasFooter" story={stories.HasFooter} />
</Canvas>

## Images
Cards include a few options for working with images. Choose from appending “image caps” at either end of a card, overlaying images with card content use for it `<Card.Img variant="top | bottom" src="your_src">`, or simply embedding the image in a card.

<Canvas>
    <Story name="HasImage" story={stories.HasImage} />
</Canvas>
