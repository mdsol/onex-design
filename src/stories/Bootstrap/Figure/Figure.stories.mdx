import { Story, Canvas, ArgsTable, Meta, Stories } from '@storybook/addon-docs';

import { Figure } from '../../../components';

<Meta
    title="Bootstrap/Components/Figure"
    component={Figure}
/>

export const Template = (args) => <Figure {...args} />;

## Figure

Anytime you need to display a piece of content, like an image with an optional caption, consider using a `Figure`.

## FigureImage
Use component `<Figure.Image>` to add image.

## FigureCaption
Use component `<Figure.Caption>` to add caption.

<Canvas>
    <Story
        name="Demo"
        args={{
            children: (
                <>
                    <Figure.Image
                        width={171}
                        height={180}
                        alt="171x180"
                        src="https://robohash.org/set_set1/bgset_bg2/3425567567456?size=300x300"
                    />
                    <Figure.Caption>
                        Nulla vitae elit libero, a pharetra augue mollis interdum.
                    </Figure.Caption>
                </>
            )
        }}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Demo" />
